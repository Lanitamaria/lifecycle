<div class="card shadow-sm mt-5 pt-3">
  <div class="card-body">
    <h1>current_user.fullname</h1>
    <ul class="nav nav-tabs mt-5" id="myTab" role="tablist">
        <li class="nav-item" role="presentation">
          <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true">
              <%= fa_icon "user" %> Acerca de mi
          </a>
        </li>
        <li class="nav-item" role="presentation">
          <a class="nav-link" id="profile-tab" data-toggle="tab" href="#profile" role="tab" aria-controls="profile" aria-selected="false">
              <%= fa_icon "brain" %> Habilidades
          </a>
        </li>
        <li class="nav-item" role="presentation">
          <a class="nav-link" id="contact-tab" data-toggle="tab" href="#contact" role="tab" aria-controls="contact" aria-selected="false">
              <%= fa_icon "briefcase" %> Experiencia de trabajo
          </a>
        </li>
    </ul>
    <div class="tab-content mt-3" id="myTabContent">
      <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
        <h2>Cuéntanos de ti</h2>
        <p>Cuéntanos un poco más acerca de ti y podremos entregarte mejores resultados en tus posibilidades laborales</p>
        <%= render partial: 'devise/registrations/edit' %>
      </div>
      <div class="tab-pane fade" id="profile" role="tabpanel" aria-labelledby="profile-tab">
        <%= render partial: 'skills/form', locals: { skill: Skill.new} %>
        <%= render partial: 'skills/show', locals: { skill: @skill} %>
      </div>
      <div class="tab-pane fade" id="contact" role="tabpanel" aria-labelledby="contact-tab">..........</div>
    </div>
  </div>
  
</div>

